import 'package:flutter/material.dart';

class VehicleContent extends StatelessWidget {
  const VehicleContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ini bagian vehicle content'),
      ),
    );
  }
}
